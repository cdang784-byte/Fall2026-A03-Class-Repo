@* Folder-level layout: every page in this Admin/ folder uses AdminLayout by default,
   with no per-page @layout needed. An _Imports.razor applies to its own folder and below. *@
@layout AdminLayout
